package com.pavelclaudiustefan.shadowapps.showstracker.ui.auth;

/**
 * Used to allow an interaction in this fragment to be communicated to the parent activity
 */
interface OnFragmentInteractionListener {
    void changeToSection(int page);
}
