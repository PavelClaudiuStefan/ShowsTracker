<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pavelclaudiustefan.shadowapps.showstracker">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".ui.movies.MoviesActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.shows.ShowsActivity"
            android:theme="@style/AppTheme.NoActionBar"/>

        <activity android:name=".ui.movies.MovieActivitySQL" />

        <activity android:name=".ui.movies.MovieActivityHTTP" />

        <activity android:name=".ui.SearchActivity" />

        <activity android:name=".ui.shows.ShowActivityHTTP" />


        <provider
            android:name=".data.VideoItemProvider"
            android:authorities="com.pavelclaudiustefan.shadowapps.showstracker"
            android:exported="false" />

    </application>

</manifest>